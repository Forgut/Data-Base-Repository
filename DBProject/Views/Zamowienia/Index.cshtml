@model IEnumerable<DBProject.Models.Zamówienia>

@{
    ViewBag.Title = "Zamówienia";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Dodaj nowe", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Data_złożenia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data_odbioru)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Odbiorcy.Nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Stan_zamówienia.Nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Data_złożenia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Data_odbioru)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Odbiorcy.Nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Stan_zamówienia.Nazwa)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.Id }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
